name: Threatbook Auto Workflow

on:
  workflow_dispatch: 
  push:
    branches:
      - main
  schedule:
    - cron: '0 0 * * *'  # 每天午夜运行一次

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Set environment variable
      run: echo "THREATBOOK_COOKIE=${{ secrets.THREATBOOK_COOKIE }}" >> $GITHUB_ENV

    - name: Debug cookie length (safe)
      run: |
        python - << 'PY'
        import os
        c = os.environ.get("THREATBOOK_COOKIE", "")
        print("THREATBOOK_COOKIE length:", len(c))
        print("THREATBOOK_COOKIE startswith:", c[:10])
        PY

    - name: Run script
      run: python threatbook_auto.py
